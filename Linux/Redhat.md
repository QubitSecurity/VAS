# Linux (Unix)

### 리눅스 서버 취약점 분석 · 평가 항목

---

#### 계정 관리

#### `R-01` `상` root 계정 원격 접속 제한

#### `R-02` `상` 패스워드 복잡성 설정

#### `R-03` `상` 계정 잠금 임계값 설정

#### `R-04` `상` 패스워드 파일 보호

#### `R-05` `상` root 이외의 UID가 '0' 금지

#### `R-06` `상` root 계정 su 제한

#### `R-07` `중` 패스워드 최소 길이 설정

#### `R-08` `중` 패스워드 최대 사용 기간 설정

#### `R-09` `중` 패스워드 최소 사용 기간 설정

#### `R-10` `중` 불필요한 계정 제거

#### `R-11` `중` 관리자 그룹에 최소한의 계정 포함

#### `R-12` `중` 계정이 존재하지 않는 GID 금지

#### `R-13` `중` 동일한 UID 금지

#### `R-14` `중` 사용자 홈 디렉토리 소유자 및 권한 설정

#### `R-15` `중` 홈 디렉토리로 PATH 디렉토리 지정 금지

#### `R-16` `상` $HOME/.rhosts, hosts.equiv 사용 금지

#### `R-17` `상` 접속 IP 및 포트 제한

#### `R-73` `상` 비밀번호 재사용 방지 설정

#### `R-74` `상` 빈 비밀번호 사용 금지

---

#### 파일 및 디렉토리 관리

#### `R-18` `상` root 홈·PATH 디렉토리 권한 및 PATH 설정

#### `R-19` `상` 파일 및 디렉토리 소유자 설정

#### `R-20` `상` /etc/passwd 파일 소유자 및 권한 설정

#### `R-21` `상` /etc/shadow 파일 소유자 및 권한 설정

#### `R-22` `상` /etc/hosts 파일 소유자 및 권한 설정

#### `R-23` `상` /etc/(x)inetd.conf 파일 소유자 및 권한 설정

#### `R-24` `상` /etc/rsyslog.conf 파일 소유자 및 권한 설정

#### `R-25` `상` /etc/services 파일 소유자 및 권한 설정

#### `R-26` `상` SUID, SGID, Sticky bit 설정 파일 점검

#### `R-27` `상` 사용자·시스템 시작파일 및 환경파일 소유자 및 권한 설정

#### `R-28` `상` world writable 파일 점검

#### `R-29` `하` /dev에 존재하지 않는 device 파일 점검

#### `R-30` `상` $HOME/.netrc 사용 금지

#### `R-31` `중` 흔적 숨기기 방지

---

#### 서비스 관리

#### `R-32` `상` Finger 서비스 비활성화

#### `R-33` `상` Anonymous FTP 비활성화

#### `R-34` `상` r 계열 서비스 비활성화

#### `R-35` `상` cron 파일 소유자 및 권한 설정

#### `R-36` `상` DoS 공격에 취약한 서비스 비활성화

#### `R-37` `상` NFS 서비스 비활성화

#### `R-38` `상` NFS 접근 통제

#### `R-39` `상` automountd 제거

#### `R-40` `상` RPC 서비스 확인

#### `R-41` `상` NIS, NIS+ 점검

#### `R-42` `상` tftp, talk 서비스 비활성화

#### `R-43` `상` Sendmail 버전 점검

#### `R-44` `상` 스팸 메일 릴레이 제한

#### `R-45` `상` 일반 사용자의 Sendmail 실행 방지

#### `R-46` `중` DNS 보안 버전 패치

#### `R-47` `상` DNS Zone Transfer 설정

#### `R-48` `중` Apache 웹 서비스 정보 숨김

#### `R-49` `상` Apache 디렉토리 리스팅 제거

#### `R-50` `상` Apache 웹 프로세스 권한 제한

#### `R-51` `상` Apache 상위 디렉토리 접근 금지

#### `R-52` `상` Apache 불필요한 파일 제거

#### `R-53` `상` Apache 링크 사용 금지

#### `R-54` `중` Apache 파일 업로드 및 다운로드 제한

#### `R-55` `상` Apache 웹 서비스 영역의 분리

#### `R-75` `상` SSH 접근 가능 사용자/그룹 제한

#### `R-76` `중` 원격 세션 타임아웃 (TMOUT) 설정

#### `R-77` `상` 웹 서비스(Apache/Nginx) 실행 여부 점검

#### `R-78` `상` CGI 실행 제한 및 불필요한 모듈 비활성화

#### `R-79` `상` 웹 설정 파일 및 DB 연결 정보 접근 통제

#### `R-80` `상` 불필요한 Alias(가상 디렉터리) 설정 제거

#### `R-81` `상` 미사용 스크립트 매핑 및 불필요한 확장자 실행 제한

#### `R-82` `상` SSI(Server-Side Includes) 및 쉘 호출 기능 제한

#### `R-83` `상` WebDAV 기능 비활성화

#### `R-84` `상` 불필요한 HTTP Method(PUT, DELETE, TRACE 등) 차단

---

#### 보안 관리

#### `R-56` `상` 최신 보안 패치 및 벤더 권고사항 적용

#### `R-57` `중` 로그온 시 경고 메시지 설정

#### `R-58` `중` NFS 설정파일 접근 권한

#### `R-59` `상` 취약한 프로그램 및 서비스 점검

#### `R-60` `중` SNMP 설정파일 접근 권한

#### `R-61` `중` SNMP 서비스 구동 점검

#### `R-62` `중` SNMP 커뮤니티 스트링의 복잡성 설정

#### `R-63` `상` 로그온 실패 로그 기록 및 확인

#### `R-64` `상` 원격 로그 서버 사용

#### `R-65` `하` su 로그 기록 및 확인

#### `R-66` `상` Syslog 설정

#### `R-67` `중` 정책에 따른 시스템 로깅 설정

#### `R-68` `상` wtmp, utmp, btmp 로그 설정

#### `R-69` `중` 보안 위협에 대한 위험 해소

#### `R-70` `상` Telnet 서비스 비활성화

#### `R-71` `상` FTP 서비스 보안 설정

#### `R-72` `상` SSH 보안 설정

#### `R-85` `중` 자동 시작 프로그램 목록 점검

#### `R-86` `중` cron 작업 내 의심스러운 명령어 점검

#### `R-87` `상` Samba 공유 및 익명 접근 제한

#### `R-88` `중` 디스크 암호화 사용 여부 점검 (LUKS/dm-crypt)

#### `R-89` `상` 감사 로그 디스크 Full 정책 설정

#### `R-90` `하` 로그 파일 순환 정책 (logrotate) 설정

#### `R-91` `중` 마지막 업데이트 이력 점검

#### `R-92` `중` 서비스 실행 현황 전수 점검
