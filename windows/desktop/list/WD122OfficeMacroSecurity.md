### **WD122 Office 매크로 보안 설정 (인터넷 차단)**

**WD122 (WD122OfficeMacroSecurity)** 항목은 **"인터넷에서 다운로드한 Office 파일의 매크로 실행 차단"**을 의미합니다.

한마디로 요약하면 **"외부(인터넷, 이메일 등)에서 가져온 엑셀이나 워드 파일에 숨겨진 악성 코드가 자동으로 실행되지 않도록 'MOTW(Mark of the Web)' 속성을 이용해 원천 봉쇄하라"**는 뜻입니다.

---

### 1. 핵심 의미: "외부 유입 파일에 대한 엄격한 격리"

MS 오피스 파일에는 업무 자동화를 위한 **매크로(VBA)** 기능이 포함될 수 있습니다. 해커들은 이를 악용하여 파일을 여는 순간 악성코드가 설치되도록 유도합니다.

* **취약한 상태 (사용자 선택에 의존):**
* 인터넷에서 받은 파일을 열면 상단에 "콘텐츠 사용" 버튼이 나타납니다.
* 사용자가 부주의하게 이 버튼을 클릭하는 순간, 매크로에 심어진 악성 스크립트가 실행되어 PC의 파일을 암호화하거나 정보를 탈취합니다.


* **안전한 상태 (시스템이 원천 차단):**
* 인터넷에서 다운로드된 파일임이 감지되면(Mark of the Web), 아예 "콘텐츠 사용" 버튼 자체가 나타나지 않습니다.
* 사용자가 실행하고 싶어도 시스템 정책에 의해 매크로 실행이 거부됩니다.



---

### 2. 점검 목적 (Why?)

"매크로 기능을 아예 못 쓰게 하는 건가요?"

아닙니다. 내부에서 직접 만든 안전한 파일은 허용하되, **출처가 불분명한 외부 유입 파일**만 타겟팅하여 차단하는 것이 목적입니다.

1. **랜섬웨어 및 이모텟(Emotet) 방어:**
* 가장 흔한 해킹 수법인 '송장(Invoice)', '이력서' 등으로 위장한 오피스 악성 파일 공격을 무력화합니다.


2. **사회 공학적 기법 무력화:**
* "내용을 보려면 위 노란색 바의 '콘텐츠 사용'을 누르세요"라는 해커의 지시를 사용자가 따를 수 없도록 UI 자체를 차단합니다.


3. **내부 자산 보호:**
* 매크로를 통한 명령 프롬프트(CMD) 호출, 파워셸(PowerShell) 실행 등 시스템 권한 탈취 시도를 입구에서 막습니다.



---

### 3. 점검 기준 (Pass/Fail)

보안 가이드라인 및 최신 MS 보안 권고안에 따른 판단 기준입니다.

* **양호 (Safe):**
* 그룹 정책에서 "인터넷에서 가져온 Office 파일에서 매크로 실행 차단" 설정이 **"사용(Enabled)"**으로 되어 있는 경우


* **취약 (Vulnerable):**
* 해당 정책이 설정되어 있지 않아, 사용자가 인터넷에서 받은 파일의 매크로를 임의로 활성화할 수 있는 경우



---

### 4. 설정 확인 및 조치 방법 (그룹 정책 관리자)

이 설정은 오피스 프로그램(엑셀, 워드, 파워포인트) 각각에 대해 적용하거나 AD(Active Directory) 환경에서 일괄 적용합니다.

1. **실행:** `Win + R` > `gpedit.msc` 입력
2. **경로 이동:** (예: Excel의 경우)
* `사용자 구성` > `관리 템플릿` > `Microsoft Excel 2016(또는 최신 버전)` > `Excel 옵션` > `보안 센터`


3. **정책 찾기:**
* **"인터넷에서 가져온 Office 파일에서 매크로 실행 차단 (Block macros from running in Office files from the Internet)"** 항목을 찾아 더블 클릭합니다.


4. **조치 (설정):**
* **[사용(Enabled)]**을 선택합니다.
* 확인을 눌러 적용합니다. (Word, PowerPoint 등 타 앱도 동일하게 반복)



---

### 💡 참고: Mark of the Web (MOTW)이란?

윈도우가 인터넷에서 다운로드된 파일에 붙이는 일종의 **'꼬리표'**입니다. 이 정책이 활성화되면 오피스 프로그램은 파일에 붙은 꼬리표를 확인하고, 외부 출처일 경우 보안 경고 대신 **빨간색 차단 막대**를 띄우며 기능을 중지시킵니다.

### 요약

> **WD122 항목은 "사용자의 실수나 호기심이 보안 사고로 이어지지 않도록, 인터넷표(MOTW) 파일의 매크로 실행 스위치를 아예 제거하라"는 뜻입니다.**

---